# Placeholder README for ImgWindow sample code

This folder contains at least two things:

1. `InitializeImGui.cpp`: a sample global initialization file for plugins to set up the font atlas for `ImgWindow` to use.
2. `fonts/`: a directory containing some example fonts that were downloaded from Google Fonts and turned into `.inc` files to use with the `ImgFontAtlas` (loaded wtihin the above example code).

... more soon ...
